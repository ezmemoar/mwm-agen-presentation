// Slidev automatically loads `styles/index.ts`.
// Order matters: component styles first, Tailwind utilities last, so a utility
// class in a template can always override a component rule.
import './slide.css'
import './ui.css'
import './tailwind.css'
